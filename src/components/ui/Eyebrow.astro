---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class={`font-mono text-xs font-medium tracking-wider text-gold-bright ${className}`}>
  <span aria-hidden="true">// </span><slot />
</span>
