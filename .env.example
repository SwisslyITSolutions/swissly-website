# Endpoint des Cloudflare Workers, der das Kontaktformular entgegennimmt.
# Lokal kopieren zu .env (wird nicht committet).
# Falls leer/fehlend: Form zeigt einen statischen Hinweis statt zu submitten.
PUBLIC_CONTACT_ENDPOINT=https://swissly-contact.example.workers.dev
